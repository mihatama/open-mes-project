# Django Settings
SECRET_KEY=django-insecure-change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,backend
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Database Settings
# Note: Ensure these match the values used in the db service or external database
POSTGRES_DB=open_mes
POSTGRES_USER=django
POSTGRES_PASSWORD=postgres
DATABASE_URL=postgres://django:postgres@db:5432/open_mes

# SSL Configuration (for compose.https.yml)
# Set to 1 for staging (test) certificates, 0 for production.
# WARNING: Always start with 1 to avoid hitting Let's Encrypt rate limits.
CERTBOT_TEST_CERT=1
DOMAIN=example.com
EMAIL=admin@example.com
